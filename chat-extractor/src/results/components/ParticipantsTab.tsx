import React from 'react';
import { ExtractionStats, senderColor } from '../../core/stats';
import { IconUsers } from '../icons';
export function ParticipantsTab({stats}:{stats:ExtractionStats}){if(!stats.participants.length)return <div className="ce-empty ce-panel-empty"><IconUsers/><p>No sender information was found in this extraction.</p></div>;const max=stats.participants[0].messageCount;return <div className="ce-participants">{stats.participants.map((p,i)=>{const c=senderColor(p.name);const pct=stats.totalMessages?((p.messageCount/stats.totalMessages)*100).toFixed(1):'0.0';return <div className="ce-participant" key={p.name}><span className="ce-rank">{i+1}</span><span className={`ce-avatar ${c.badge}`}>{p.name.charAt(0).toUpperCase()}</span><div className="ce-participant-main"><strong className={c.text}>{p.name}</strong><div className="ce-progress"><i className={c.text} style={{width:`${Math.round(p.messageCount/max*100)}%`}}/></div></div><div className="ce-participant-count"><strong>{p.messageCount.toLocaleString()}</strong><small>{pct}%</small></div></div>})}</div>}
